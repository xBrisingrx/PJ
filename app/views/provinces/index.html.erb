<h1>Provincias</h1>
<%= link_to "<i class='fa fa-plus'></i> Registrar provincia".html_safe, new_province_path(),remote: true, data: { toggle: :modal, target: '#modal' }, 
  class: 'btn u-btn-outline-primary u-btn-hover-v1-2 mb-2' %>

<%= render( partial: 'shared/table', 
            locals: { table_id: 'provinces', 
                      color: 'blue', 
                      icon: 'fa-gear',
                      title: "Provincias registradas", 
                      col_names: [ 'Nombre', 'Descripcion', 'Acciones' ] }) %>

<!-- Modal para eliminar province -->
<div class="modal fade" id="modal-disable-province" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">¿ Esta seguro de dar de baja esta empresa ?</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <%= form_with(url: disable_province_path, id: 'form-disable-province') do |form| %>
          <%= form.hidden_field :province_id %>
          <%= form.submit 'Eliminar', class: 'btn u-btn-red g-mr-10' %>
          <button type="button" data-dismiss="modal" class="btn u-btn-teal g-mr-10"> Cerrar </button>
        <% end %>
      </div>
    </div>
  </div>
</div>
<!-- Fin modal eliminar province -->
